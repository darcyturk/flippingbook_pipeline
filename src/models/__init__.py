# src/models/__init__.py

# This file makes the model classes directly available from the 'models' package.
# Instead of: from src.models.aspect_models import ProductDetailsAspect
# We can now use: from src.models import ProductDetailsAspect

from .aspect_models import ProductDetailsAspect

